# Build stage
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY . .
RUN go build -o newsletter-worker ./cmd/worker

# Final image
FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/newsletter-worker .
CMD ["./newsletter-worker"]
